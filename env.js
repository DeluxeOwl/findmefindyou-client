// Setting env variables in expo is finnicky, might as well use this

const BACKEND_URL = "http://192.168.0.105:8000";
const UPDATE_REMOTE_DB_INTERVAL_SECONDS = 60;
const FOREGROUND_LOCATION_SECONDS = 60;

export default {
  BACKEND_URL,
  UPDATE_REMOTE_DB_INTERVAL_SECONDS,
  FOREGROUND_LOCATION_SECONDS,
};
